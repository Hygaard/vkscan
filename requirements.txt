# Runtime dependencies
Pillow>=10.0.0
imagehash>=4.3.0
send2trash>=1.8.0  # Safe deletion (trash/recycle bin)

# Optional: document similarity (PDF, DOCX, XLSX, ODS)
pdfplumber>=0.10.0  # PDF text extraction
python-docx>=1.0.0  # DOCX text extraction
openpyxl>=3.1.0     # XLSX text extraction
odfpy>=1.4.0        # ODS/ODT text extraction

# Optional dependencies
# tkinterdnd2>=0.3.0  # Native drag-and-drop (pip install tkinterdnd2)

# Build dependency (optional - only needed for creating executable)
# pyinstaller>=6.0.0
